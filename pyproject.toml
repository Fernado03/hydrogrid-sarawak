[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hydrogrid"
version = "0.1.0"
description = "HydroGrid Sarawak: Renewable Energy & Green Hydrogen Platform"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]